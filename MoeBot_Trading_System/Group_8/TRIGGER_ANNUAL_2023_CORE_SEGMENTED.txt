Trigger canonical FREE-only exact segmented Annual Core 2023 recovery from verified checkpoint 2.
Stages 4, 5, 6 and 7 execute one-per-free-job with SHA-bound checkpoint chaining.
Run 30528404287 restored and verified checkpoint 2, then exact stage 4 reached the 360-minute free-job ceiling without a semantic assertion failure and produced no checkpoint-3 artifact.
The canonical fast path remains scoped by frozen symbol/timeframe catalogs for Group 6 and Group 7 boundaries.
The exact stage-4 Group 4 and Group 7 lifecycle indexes now use bounded batched zone_id IN probes over the same existing indexes, preserving the original per-zone transition-time and transition-ID/ordinal ORDER BY tie-breaks, same-time collapse behavior, status logic, IDs and causal timestamps while eliminating thousands of connection round trips.
Connection-local SQLite cache, mmap, in-memory temp/journal and synchronous settings remain physical-only and do not change persisted rows, frozen semantics, configuration, schema, thresholds, IDs or causal timestamps.
Frozen 2024 OOS access remains locked.
